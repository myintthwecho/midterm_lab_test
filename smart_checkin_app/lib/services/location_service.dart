class LocationService {
  // Responsible for retrieving the device's current GPS coordinates.
  // Will use the geolocator package to request location permissions
  // and return latitude/longitude for check-in verification.
}
